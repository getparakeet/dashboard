doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Log In!
    body
    h2 login
        form(action="/api/flow/in", method="post")
            input#em(type="email", name="eid" placeholder="email" required)
            button(type="submit") submit